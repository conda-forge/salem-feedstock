{% set name = "salem" %}
{% set version = "0.3.5" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/salem-{{ version }}.tar.gz
  sha256: 8be58294547a40360842cfa0d7e6a159ed1d3fdb627e09f87ed8d5dae57f14ee

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.5
  run:
    - python >=3.5
    - joblib
    - netCDF4
    - numpy
    - pandas
    - pyproj
    - scipy
    - six
    - xarray

test:
  imports:
    - salem
    - salem.tests
  commands:
    - pip check
  requires:
    - pip

about:
  home: http://salem.readthedocs.io
  summary: Geoscientific data I/O and map projections
  license: BSD-3-Clause
  license_file: LICENSE.rst

extra:
  recipe-maintainers:
    - fmaussion
    - TimoRoth
    - raybellwaves
